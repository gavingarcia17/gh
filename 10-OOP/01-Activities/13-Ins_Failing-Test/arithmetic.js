// A class Arithmetic is created.
class Arithmetic {
  // Class Arithmetic is given a sum method that returns two numbers added together.
  sum(a, b) {
    return a + b;
  }

  // Class Arithmetic is given a difference method that returns two numbers subtracted.
  difference(a, b) {
    return a - b;
  }

  // Class Arithmetic is given a product method that returns two numbers multiplied together.
  product(a, b) {
    return a * b;
  }

  // Class Arithmetic is given a quotient method that returns two numbers divided.
  quotient(a, b) {
    return a / b;
  }

  // Class Arithmetic is given a modulus method that currently doesn't do anything.
  modulus() {}
}

// Class Arithmetic is exported from the file.
export default Arithmetic;
